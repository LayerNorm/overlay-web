# Optional operational override. Browser enrollment stores the short-lived
# host credential under StateDirectory, so ordinary installations leave this unset.
# OVERLAY_AGENT_HOST_CREDENTIAL=

# Used only when an Eve adapter declares bearerTokenEnv: EVE_AGENT_TOKEN.
# EVE_AGENT_TOKEN=
